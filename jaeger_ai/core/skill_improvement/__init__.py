"""Skill self-improvement subsystem — the per-use notes journal, the
revision log, and skill lifecycle (archive + scoring/retirement). The trigger +
the second-person review live in ``agent/background/skill_review.py`` (they ride
the Deep Think loop); these are the core data + lifecycle modules they build on.
See dev/docs/history/SKILL_EVOLUTION_PLAN.md."""
