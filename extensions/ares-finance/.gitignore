# Financial data — NEVER commit. The SQLite store holds synced account
# balances and the transaction ledger; this repo is public.
server/data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Credentials
.env
.env.local

# Monarch session/token caches written by the SDK
.mm/
*.session
monarch_session.pickle

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/

# macOS
.DS_Store
