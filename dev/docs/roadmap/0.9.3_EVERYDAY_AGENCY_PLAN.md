# JaegerAI 0.9.3 — everyday agency (the "it should just do things" sprint)

> From the operator's 0.8 field brief (dev tasks failing: open YouTube in
> Safari, make/send email; post-boot setup amnesia) + full recon. The recon's
> headline: THE TOOLS EXIST — tier-2 actions die at ConsoleConfirmationProvider
> failing closed on non-tty stations (permissions.py:281 — GUI/voice stations
> can never say yes). Skills are the second half, not the first.

## Task 1 — the headless confirmation surface (unblocks ALL app control)
Route tier-2 confirmations to whatever surface the operator is actually on:
bridge stations get a protocol approval frame → Swift approval dialog (the
mid-turn approval machinery from messaging ALREADY flows AgentRequest over
the bridge — reuse that seam); voice-only asks aloud (speak + yes/no listen)
where feasible, else queues a visible pending approval; tty keeps console.
Persisted per-skill grants (permissions.json "always") already work — the
dialog offers once/always/deny. Fail-closed REMAINS the fallback when no
surface can ask. Walk: a real bridge session approving open_on_host once,
"always" persisting across restart.

## Task 2 — two real tools
(a) send_email: modeled on messaging.py send_message; Apple-native first
(Mail.app via the existing applescript_engine), himalaya CLI as alt backend;
tier-2. (b) move_file/copy_file beside files.py's existing set (shutil,
same sandbox+tiering). Tests per house pattern.

## Task 3 — self-model: setup-state + app-control awareness
build_self_model_block() gains: (a) configured-state — intersect installed
channels with autostart + credential status → "messaging: telegram ✓ active,
discord ✗ (needs token)"; (b) an app-control group (computer_use/browser/
host) so the id KNOWS it can drive the desktop; (c) boot injects the active-
channels line (data already computed by autostart_plugins). Kills post-boot
setup amnesia at the prompt level.

## Task 4 — the skill documents (operator's brief)
Author SOP playbooks (scheduling/messaging-SOP style, each with eval rows):
web-research (web_search→web_extract triage chain), email-triage (+ the new
tool), file-organization (+ the new tools), process-monitoring (run_shell
recipes now; dedicated tool later), log-calculations (parse→execute_code).
App-driving SKILL.md exists (apple/macos-computer-use) — extend with the
YouTube-class one-shots via open_on_host.

## Gates
Suites; persona/delegation eval intact; NEW everyday-tasks eval lane: the
operator's literal failures as cases ("open youtube in safari" → open_on_host
fires + approval flow; "is telegram set up" → self-model answers without
delegation... no: SELF-STATE still delegates for TRUTH, but the block gives
grounding; "send an email" → tool path). Bench ≥79/81. Walk Task 1 on a real
bridge session.
